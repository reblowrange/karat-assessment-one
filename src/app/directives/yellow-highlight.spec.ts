import { YellowHighlight } from './yellow-highlight';

describe('YellowHighlight', () => {
  it('should create an instance', () => {
    const directive = new YellowHighlight();
    expect(directive).toBeTruthy();
  });
});
